#include "ltbm.h"

int ltbm::arrsize(int arr[]){
	return sizeof(arr)/sizeof(arr[0]);
}

int ltbm::arrsize(char arr[]){
	return sizeof(arr)/sizeof(arr[0]);
}

int ltbm::arrsize(float arr[]){
	return sizeof(arr)/sizeof(arr[0]);
}

int ltbm::arrsize(double arr[]){
	return sizeof(arr)/sizeof(arr[0]);
}

/*
int ltbm::arrsize(std::string arr[]){
	return sizeof(arr)/sizeof(arr[0]);
}
*/

